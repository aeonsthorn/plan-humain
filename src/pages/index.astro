---
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE_DESCRIPTION } from "../config";
import I from "../../utils/translate";
const lang = "fr";
---

<!DOCTYPE html>
<html lang={lang}>
  <head>
    <BaseHead title="Accueil" description={SITE_DESCRIPTION} />
  </head>
  <body>
    <Header />
    <main
      class="text-custom-beige-100 max-w-[1300px] m-auto px-4 sm:px-12 py-10 h-full"
    >
      <div class="relative flex h-full">
        <div
          class="absolute bg-gradient-to-r from-custom-green-900 to-transparent w-full h-full z-10 opacity-40"
        >
        </div>
        <div
          class="w-full min-h-[300px] sm:min-h-[350px] md:min-h-[450px] h-full"
        >
          <picture>
            <source
              srcset="/heroImageSmall.webp"
              media="(max-width: 650px)"
              type="image/webp"
            />
            <source
              srcset="/heroImageSmall.jpg"
              media="(max-width: 650px)"
              type="image/jpg"
            />
            <img
              src="/heroImage.jpg"
              alt="plan humain logo"
              class="h-[300px] sm:h-full object-cover"
            />
          </picture>
        </div>
        <div class="absolute top-[20%] left-[8%] z-30">
          <h1>
            {I(" Votre facon", lang)}<br />
            {I("de faire partie", lang)}<br />
            {I("de la solution", lang)}
          </h1>
          <a href="/services" class="">
            <div
              class="w-fit flex items-center relative mt-5 p-2 px-4 pr-5 border-2 rounded-full border-custom-beige-100"
            >
              <p class="font-semibold">{I("Nos services", lang)}</p>
              <picture class="absolute w-fit right-3">
                <img src="/arrow.png" class="w-1 mt-1" alt="right arrow" />
              </picture>
            </div>
          </a>
        </div>
      </div>

      <p class="px-4 sm:px-10 md:px-20 pt-10 leading-relaxed">
        {
          I(
            "Avec Plan humain, l’engagement social dépasse les bonnes intentions et les campagnes de communication. Notre équipe accompagne les leaders dans l’adoption d’une approche structurée, viable à long terme et créatrice de valeur à chaque point de contact. Un geste à la fois, nous permettons aux organisations de s’engager à prospérer en changeant des vies pour le mieux. S’engager à élever la barre. S’engager à inspirer.",
            lang
          )
        }
      </p>
      <div class="flex justify-center py-10">
        <a
          href="/aboutUs"
          class="w-fit relative flex items-center border-2 border-custom-beige-100 p-4 pr-6 pl-4 rounded-full"
        >
          <h6 class="w-full uppercase">
            {I("Équipe et collaborateurs", lang)}
          </h6>
          <picture class="absolute w-fit right-4 pb-1">
            <img src="/arrow.png" class="w-1 mt-1" alt="right arrow" />
          </picture>
        </a>
      </div>
    </main>
    <Footer lang={lang} />
  </body>
</html>
